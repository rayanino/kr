# خزانة ريان (KR) — Python dependencies
# Install: pip install -r requirements.txt

# ── Core ──
pydantic>=2.0
pydantic-settings>=2.0

# ── LLM Integration (D-041: consensus via Instructor + from_provider) ──
litellm>=1.40.0
instructor[anthropic]>=1.12.0
anthropic>=0.30.0

# ── Encoding Detection (SPEC §4.A.2 Step 3) ──
charset-normalizer>=3.0

# ── Web GUI (D-043: FastAPI + HTMX) ──
fastapi>=0.100
uvicorn>=0.20
jinja2>=3.0
python-multipart>=0.0.5

# ── Arabic Text Processing ──
pyarabic>=0.6
# camel-tools — install separately: pip install camel-tools
# sentence-transformers — deferred: passaging §4.B.1 quality prediction, §4.B.2 implicit structure

# ── OCR (D-028) ──
# mistralai — for Mistral OCR API
# pillow — image processing for scanned sources

# ── Data Processing ──
beautifulsoup4>=4.12
lxml>=4.9
httpx>=0.24.0

# ── Testing ──
pytest>=7.0
pytest-asyncio>=0.21

# ── Concurrency ──
filelock>=3.0

# ── Utilities ──
PyYAML>=6.0
python-dotenv>=1.0
