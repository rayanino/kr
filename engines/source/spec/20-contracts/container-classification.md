# ContainerClassification

Owned by:

- `30-container-classification`

Purpose:

- Describe the uploaded container without normalizing its text.

Core fields:

- `container_type`
- `normalization_route`
- `volume_manifest`
- `supplementary_members`
- `text_encoding`

Rules:

- Container classification is provisional where later intake analysis must refine PDF-specific routing evidence.
