# Strategic Audit of the Excerpting Engine Improvement Portfolio

## Executive summary

The excerpting engine is already architected with strong “contracts → phases → validation” discipline, but the current implementation exhibits a *high-impact failure signature*: **excerpts are being silently dropped at validation time**, including structurally crucial micro-units like enumerated headings (“الثالثة”, “الرابعة”, …) and section openers (“المسألة الخامسة…”). This is visible in real production-like runs where **EX‑V‑002 (primary text integrity) repeats across packages** and a nontrivial set of units appear in Phase 2b artifacts but never reach the final excerpts (“ANO‑UNIT‑LOSS”). fileciteturn28file0L1-L1 fileciteturn31file0L1-L1

The root cause is *mechanical and fixable*: deterministic assembly writes `ExcerptRecord.text_snippet` from `TeachingUnit.text_snippet` (LLM-supplied), while validation uses `text_snippet` as the anchor for V‑P3‑2 and drops excerpts when the normalized prefix check fails **or when the snippet is < 20 chars**—which guarantees drops for “micro” units. fileciteturn34file0L1-L1 fileciteturn32file0L1-L1

The highest leverage improvements therefore cluster into three themes: (1) eliminate silent dropping via deterministic snippet derivation and better integrity semantics, (2) prevent “micro-unit” pedagogical damage by merging/attaching structural markers rather than emitting (or dropping) them, and (3) harden the system so future corruption cannot pass unnoticed (drop ledgers + smoke-regression). fileciteturn29file0L1-L1 fileciteturn33file0L1-L1

Scoring and ranking below uses: **Score = (Impact × Feasibility) / ComplexityWeight**, with ComplexityWeight: Low=1, Medium=2, High=3.

## Evidence base and scoring method

Primary evidence was taken from the excerpting engine’s specification and implementation, including deterministic assembly (Phase 3), validation logic, and production-like smoke outputs committed under `integration_tests/smoke_api_v2/`. fileciteturn4file0L1-L1 fileciteturn34file0L1-L1 fileciteturn32file0L1-L1 fileciteturn28file0L1-L1

Where possible, improvements are grounded in three kinds of proof:
- **Spec-to-code drift** (SPEC rules vs code paths and constants). fileciteturn4file0L1-L1 fileciteturn12file0L1-L1
- **Observed production failure modes** (EX‑V‑002 prevalence, validation drops, “ANO‑UNIT‑LOSS”). fileciteturn28file0L1-L1 fileciteturn29file0L1-L1 fileciteturn31file0L1-L1
- **Contract boundary leverage** (upstream metadata exists but is not used; downstream expects stable semantics). fileciteturn15file0L1-L1 fileciteturn12file0L1-L1 fileciteturn16file0L1-L1

## Ranked improvements for the excerpting engine

1) **Deterministic `text_snippet` + rewrite V‑P3‑2 so micro-units are not auto-dropped**  
**Ranking:** Impact 10, Feasibility 9, Complexity Low (1) → **Score 90.0**  
**A. WHAT:** In `phase3_deterministic.build_deterministic_excerpts`, set `ExcerptRecord.text_snippet = primary_text[:80]` (or an equivalent deterministic derivation) rather than `unit.text_snippet`. Then revise `phase3_validation.validate_excerpt` V‑P3‑2 so that a short excerpt does not fail solely because `compare_len < 20`. The current logic drops when `compare_len < 20` *or* when normalized prefixes differ; a correct short-text policy is: if the excerpt’s normalized `primary_text` is shorter than 20 chars, compare the full available overlap and accept exact matches. fileciteturn34file0L1-L1 fileciteturn32file0L1-L1  
**B. WHY:** This stops the engine from silently losing “structural glue” (enumeration headings, section openers, brief definitions) that students rely on to orient themselves. It also removes an LLM-precision dependency (unicode-length counting) from a determinism-critical integrity gate. fileciteturn32file0L1-L1  
**C. EVIDENCE:** Deterministic assembly currently writes `text_snippet=unit.text_snippet` (LLM-provided). fileciteturn34file0L1-L1 Validation V‑P3‑2 drops if `compare_len < 20`, which will always drop short snippets like “الثالثة”. fileciteturn32file0L1-L1 Smoke output shows repeated `EX-V-002` and dropped snippets that are exactly these short units. fileciteturn28file0L1-L1 fileciteturn29file0L1-L1 fileciteturn31file0L1-L1  
**D. DISCOVERY PATH:** A strong `code_quality` scan will surface the brittle `compare_len < 20 → drop` condition and the reliance on LLM-supplied `unit.text_snippet`. fileciteturn32file0L1-L1 fileciteturn34file0L1-L1 However, recognizing its *real-world severity* likely requires reading smoke artifacts; none of the listed scanners are guaranteed to parse `integration_tests/smoke_api_v2/` today. fileciteturn28file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Low (<1 day).  
**F. RISK IF UNFIXED:** The student’s library will contain systematic holes: headings vanish, “مسألة” openers vanish, and downstream engines (taxonomy/synthesis) will build understanding on incomplete evidence—quietly. fileciteturn31file0L1-L1

2) **Fail-fast on excerpt drops: treat V‑P3‑2 drops as run-breaking, not merely logged**  
**Ranking:** Impact 9, Feasibility 8, Complexity Medium (2) → **Score 36.0**  
**A. WHAT:** Upgrade `validate_batch` to enforce a strict invariant: **if any excerpt is dropped by V‑P3‑2, the run must surface a hard failure** (or at minimum raise a gate flag that blocks writing “final” outputs). This includes writing a structured artifact (e.g., `phase3_validation_drops.jsonl`) containing `excerpt_id`, `div_id`, `unit_index`, `text_snippet`, normalized `primary_text[:120]`, and the triggering comparison lengths—so the failure is diagnosable without log scraping. Start from the existing `drop` behavior and `dropped_count` logging, but add an explicit threshold and termination policy. fileciteturn32file0L1-L1  
**B. WHY:** A personal scholarly library cannot tolerate silent corruption: once written, missing excerpts look like “the author never said this,” which is catastrophic for study. A fail-fast policy turns hidden corruption into an actionable queue item, aligning with the project’s own testing philosophy (determinism first; don’t let broken 5a contaminate higher-level evaluation). fileciteturn33file0L1-L1  
**C. EVIDENCE:** `validate_batch` currently logs “Dropped X excerpts” but returns the shortened list, allowing output writing to proceed. fileciteturn32file0L1-L1 The smoke run summary shows high EX‑V‑002 counts across multiple packages. fileciteturn28file0L1-L1 The committed `validation_drops.jsonl` and “ANO‑UNIT‑LOSS” review candidates demonstrate that these drops translate into missing final excerpts. fileciteturn29file0L1-L1 fileciteturn31file0L1-L1  
**D. DISCOVERY PATH:** `test_health` will only surface this if tests assert “no drops allowed.” Today, the strongest signal is in smoke artifacts and `phase3_validation` itself; so `code_quality` (logic review) plus a new “smoke output audit” step would find it reliably. fileciteturn32file0L1-L1 fileciteturn28file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Medium (1–3 days).  
**F. RISK IF UNFIXED:** You will ship “apparently clean” excerpt corpora with irrecoverable omissions, and the student will build notes and mental models atop missing premises. fileciteturn31file0L1-L1

3) **Add a run-level “unit conservation” invariant to catch ANO‑UNIT‑LOSS**  
**Ranking:** Impact 9, Feasibility 7, Complexity Medium (2) → **Score 31.5**  
**A. WHAT:** Introduce a conservation check: for each `(source_id, div_id, chunk_index)`, the set of `TeachingUnit.unit_index` produced at the end of Phase 2b must map to either (a) a final `ExcerptRecord` with the same `(div_id, chunk_index, unit_index)`, or (b) an explicit drop record with error codes. This can live in the Phase 3 orchestrator after validation: compare Phase2b groupings artifacts to the final excerpts list and emit a run-breaking error if any units disappear without a drop ledger entry. fileciteturn31file0L1-L1  
**B. WHY:** This ensures the pipeline cannot silently lose “units” for *any* reason (future bugs, edge cases, or a new validation rule). For a student, missing a unit is worse than a low-quality unit: it removes the possibility of review and correction. fileciteturn33file0L1-L1  
**C. EVIDENCE:** The smoke review candidates explicitly label units as “present in Phase 2b but absent from final excerpts,” with anomaly IDs like `ANO-UNIT-LOSS`. fileciteturn31file0L1-L1 The smoke tree also includes per-chunk Phase 2b groupings JSONs where these units exist. fileciteturn27file16L1-L1  
**D. DISCOVERY PATH:** None of the standard scanners necessarily compute cross-artifact conservation unless explicitly instructed. A `test_health` scanner could find it if there were tests parsing `integration_tests/smoke_api_v2`; otherwise, a `creative` (failure-mode review) run could propose it by reading the smoke anomaly logs. fileciteturn35file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Medium (1–3 days).  
**F. RISK IF UNFIXED:** Unit loss remains a recurring “ghost bug”: you see output counts lower than expected, but you cannot prove what vanished or why without deep manual forensics. fileciteturn28file0L1-L1

4) **Merge/attach micro-structure tokens instead of emitting (or dropping) them as standalone excerpts**  
**Ranking:** Impact 8, Feasibility 7, Complexity Medium (2) → **Score 28.0**  
**A. WHAT:** Add a deterministic post-processing pass between Phase 2b and Phase 3 that detects “micro-structure units” and attaches them to the following unit’s `primary_text` (and adjusts `start_word/end_word/segment_indices`). Examples: enumerator-only units (“الأولى…”, “الثانية…”, “الثالثة…”) and terminators (“انتهى.”). This can be implemented as a rule-based merge when `len(primary_text.strip()) < N` *and* the text matches known structural patterns. fileciteturn29file0L1-L1  
**B. WHY:** Students need stable, pedagogically meaningful excerpts: an excerpt that is only “الثالثة” is not directly useful, but losing it entirely removes the structure of a list or argument. Attaching it to the next excerpt preserves both structure and readability. fileciteturn31file0L1-L1  
**C. EVIDENCE:** The validation drops contain many excerpts whose `text_snippet` is exactly “الثالثة”, “الرابعة”, “التاسعة”, etc., demonstrating that the system currently creates (and then drops) micro-units. fileciteturn29file0L1-L1 Smoke review candidates also show these as “grouped_only” units with `ANO-UNIT-LOSS`. fileciteturn31file0L1-L1 V‑P3‑2’s short-length drop threshold makes these units especially vulnerable. fileciteturn32file0L1-L1  
**D. DISCOVERY PATH:** `creative` via “local_failure_mode_review” is the most likely to spot this as a realistic failure mode and propose a bounded deterministic merge strategy. fileciteturn35file0L1-L1 Evidence from `integration_tests/smoke_api_v2` is also a strong signal, but scanners must be directed to read it. fileciteturn29file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Medium (1–3 days).  
**F. RISK IF UNFIXED:** The student repeatedly encounters excerpts missing the “names” of list items or missing the “مسألة” framing, weakening comprehension and note-taking. fileciteturn31file0L1-L1

5) **Turn `integration_tests/smoke_api_v2` into excerpting regression tests that block EX‑V‑002 and unit loss**  
**Ranking:** Impact 8, Feasibility 7, Complexity Medium (2) → **Score 28.0**  
**A. WHAT:** Add a deterministic pytest module under `engines/excerpting/tests/` that reads committed smoke outputs (e.g., `SUMMARY.json`, `validation_drops.jsonl`, and anomaly logs) and asserts: (a) `EX-V-002` count is zero (or below an explicit, justified threshold), and (b) no `ANO-UNIT-LOSS` anomalies exist. This aligns with the project’s “5a first” doctrine and converts a production failure signature into a permanent guardrail. fileciteturn33file0L1-L1  
**B. WHY:** Without tests that reflect real output behavior, the autonomous system may “fix” locally but regress later. A student-facing library needs regression certainty: the same source processed next month should not silently lose units. fileciteturn33file0L1-L1  
**C. EVIDENCE:** Smoke summary shows nonzero `EX-V-002` across four “successful” packages (and a failed package). fileciteturn28file0L1-L1 The drop and anomaly logs are already committed and therefore stable targets for regression assertions. fileciteturn29file0L1-L1 fileciteturn31file0L1-L1  
**D. DISCOVERY PATH:** `test_health` is the intended scanner: mapping SPEC rules to test coverage and identifying missing “real output” regression checks. fileciteturn22file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Medium (1–3 days).  
**F. RISK IF UNFIXED:** Fixes remain fragile; you may improve one failure mode while reintroducing another, and the student experiences unpredictable excerpt coverage across runs. fileciteturn33file0L1-L1

6) **Stop using `review_flags=["llm_enrichment_failed"]` as a pre-enrichment placeholder**  
**Ranking:** Impact 7, Feasibility 8, Complexity Medium (2) → **Score 28.0**  
**A. WHAT:** In `phase3_deterministic.build_deterministic_excerpts`, remove the “preload failure flag” behavior for `SelfContainmentLevel.PARTIAL`/`DEPENDENT`. Replace it with either a neutral state flag (e.g., `enrichment_pending`) or adjust the `ExcerptRecord` validation rules so that `context_hint=None` is allowed prior to enrichment *without* declaring enrichment failure. Then ensure enrichment success clears any pending marker and that genuine failure sets `llm_enrichment_failed`. fileciteturn34file0L1-L1  
**B. WHY:** `review_flags` are part of the student’s trust interface: if a flag says “enrichment failed” when enrichment was merely deferred, the user sees false-quality signals. Worse, `phase3_validation` uses `llm_enrichment_failed` to suppress requirements for `excerpt_topic` size and `context_hint`, meaning a placeholder can mask real corruption if it survives later phases. fileciteturn32file0L1-L1  
**C. EVIDENCE:** Deterministic assembly sets `review_flags=["llm_enrichment_failed"]` whenever self-containment is not FULL, explicitly to bypass validators until Session 4 fills fields. fileciteturn34file0L1-L1 Validation applies relaxed checks when `has_enrichment_failed` is true (topic count and context_hint requirements). fileciteturn32file0L1-L1  
**D. DISCOVERY PATH:** `code_quality` (logic smell: using a failure flag to represent “not yet done”) and `spec_quality` (SPEC semantics around failure vs pending) are the most likely. fileciteturn4file0L1-L1 fileciteturn34file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Medium (1–3 days).  
**F. RISK IF UNFIXED:** Either chronic false alarms (“enrichment failed” everywhere) or—more dangerously—validators remain permanently bypassed for partial/ dependent excerpts, allowing low-quality metadata to enter taxonomy/synthesis. fileciteturn32file0L1-L1

7) **Fix footnote relevance extraction to handle multiple occurrences (and avoid first-hit bias)**  
**Ranking:** Impact 7, Feasibility 8, Complexity Low (1) → **Score 56.0**  
**A. WHAT:** Update `filter_relevant_footnotes` so it searches **all** occurrences of `⌜{ref_marker}⌝` in `assembled_text`, not just the first `find()`. The current implementation uses `pos = assembled_text.find(pattern)` once; this can mis-assign relevance if the same marker appears multiple times or if the first occurrence is outside the current unit’s char range. Implement a while-loop (`find(pattern, start)`) and include the footnote if **any** occurrence falls inside `[char_start, char_end)`. fileciteturn34file0L1-L1  
**B. WHY:** Footnotes (especially takhrīj and editorial notes) carry scholarly metadata that students use to verify claims. Missing or wrongly attached footnotes silently degrades trust and prevents the student from following sources. fileciteturn15file0L1-L1  
**C. EVIDENCE:** `filter_relevant_footnotes` uses a single `find()` per footnote marker and decides relevance on that single position. fileciteturn34file0L1-L1 The normalization contracts show footnotes are structured objects with `ref_marker` and type, meaning excerpting is expected to preserve and localize them correctly. fileciteturn15file0L1-L1  
**D. DISCOVERY PATH:** `code_quality` (pattern: `str.find` without loop or `finditer` for repeated markers) is likely to catch this, especially under a “silent corruption hazards” rubric. fileciteturn34file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Low (<1 day).  
**F. RISK IF UNFIXED:** The student will encounter excerpts that cite evidence in footnotes but the system fails to surface the relevant footnote, undermining verification and study discipline. fileciteturn15file0L1-L1

8) **Use upstream structured takhrīj signals instead of discarding them (`takhrij_data=None`)**  
**Ranking:** Impact 8, Feasibility 6, Complexity Medium (2) → **Score 24.0**  
**A. WHAT:** Populate `ExcerptRecord.takhrij_data` deterministically when the unit contains relevant footnotes whose `footnote_type` indicates hadith takhrīj (or similar). Today, excerpting stores `footnotes_relevant` but writes `takhrij_data=None` for every excerpt. A bounded improvement is to derive a minimal `takhrij_data` summary from `Footnote.takhrij_data` when present and attach it at the excerpt level. fileciteturn34file0L1-L1  
**B. WHY:** For an Islamic studies student, the ability to see *where a hadith is sourced* is not optional enrichment; it is core study utility and prevents silent evidence corruption (e.g., treating unsourced text as sourced evidence). fileciteturn12file0L1-L1  
**C. EVIDENCE:** Excerpting explicitly sets `takhrij_data=None` while still carrying footnotes. fileciteturn34file0L1-L1 The normalization contracts define typed footnotes (including hadith/takhrīj concepts) and allow structured `takhrij_data` to flow. fileciteturn15file0L1-L1 The excerpting ExcerptRecord contract includes `takhrij_data` as a first-class field. fileciteturn12file0L1-L1  
**D. DISCOVERY PATH:** `contract_boundaries` is the natural scanner: upstream offers structured data that excerpting currently throws away (or fails to lift into the excerpt-level field). fileciteturn22file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Medium (1–3 days).  
**F. RISK IF UNFIXED:** The library trains the student to accept unsourced claims as if they were sourced; even when the source exists upstream, it is not presented at the right abstraction level in excerpting. fileciteturn15file0L1-L1

9) **Repair SPEC↔contracts drift in model defaults and verification semantics**  
**Ranking:** Impact 6, Feasibility 8, Complexity Low (1) → **Score 48.0**  
**A. WHAT:** Align the excerpting engine’s model configuration defaults across SPEC and `ExcerptingConfig`. The SPEC specifies defaults for “classify/group/enrich” vs “verify,” but the configuration currently sets `task_model= openai/gpt-5.4` while `verify_model= anthropic/claude-opus-4.6`—the inverse of what the SPEC states. Decide the intended truth and update the other artifact; then add a small test asserting the defaults match the spec (or that spec is explicitly non-authoritative for defaults). fileciteturn4file0L1-L1 fileciteturn12file0L1-L1  
**B. WHY:** Model choice is not cosmetic: it impacts Arabic handling, determinism of structured outputs, cost, and failure patterns. Silent drift means the autonomous system will optimize the wrong behaviors and the student will experience inconsistent excerpt quality across runs. fileciteturn4file0L1-L1  
**C. EVIDENCE:** The SPEC’s configuration section defines default model/provider assignments, while `ExcerptingConfig` encodes different defaults. fileciteturn4file0L1-L1 fileciteturn12file0L1-L1  
**D. DISCOVERY PATH:** `spec_quality` is almost tailored for this: it explicitly audits contradictions between SPEC and contracts/code. fileciteturn22file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Low (<1 day).  
**F. RISK IF UNFIXED:** You will chase phantom regressions (“why did excerpting change?”) caused purely by configuration drift rather than algorithmic changes, harming the student’s trust in the library. fileciteturn4file0L1-L1

10) **Make consensus disagreements actionable: include field-level diffs and stabilize error-code vocabulary**  
**Ranking:** Impact 7, Feasibility 6, Complexity Medium (2) → **Score 21.0**  
**A. WHAT:** Strengthen consensus verification reporting so that when a record gets tagged with `consensus_disagreement`, the system records *which fields disagreed*, each model’s value, and the final chosen value into `consensus_metadata` (or an adjacent artifact). Also ensure that any “consensus verification failed” error code (e.g., `EX-M-011`) is consistently defined in the SPEC’s error-code catalog and referenced by consensus code paths (and tests). fileciteturn12file0L1-L1  
**B. WHY:** Disagreement without a diff is expensive: the student (or author) cannot efficiently review what might be wrong. Actionable diffs turn “review flags” into concrete, ranked work items—directly improving the usefulness and trustworthiness of excerpt metadata. fileciteturn31file0L1-L1  
**C. EVIDENCE:** Smoke review candidates include “Consensus verification disagreed on some fields” review questions but do not embed which fields were in conflict, implying current metadata is not sufficiently explanatory for audit. fileciteturn31file0L1-L1 The contracts define a consensus-failure code (`EX-M-011`) that is not obviously represented in the SPEC’s error-code list as shown, indicating drift risk. fileciteturn12file0L1-L1 fileciteturn4file0L1-L1  
**D. DISCOVERY PATH:** `spec_quality` (error-code catalog drift) plus `creative` (failure-mode review highlighting lack of actionable evidence) are the most likely. fileciteturn35file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Medium (1–3 days).  
**F. RISK IF UNFIXED:** The student (or owner) must do costly manual review over many excerpts, and subtle attribution/topic errors can persist unnoticed because the system provides “there was disagreement” without enough forensic detail. fileciteturn31file0L1-L1

11) **Expand deterministic evidence marker detection and add tests against false negatives**  
**Ranking:** Impact 6, Feasibility 7, Complexity Low (1) → **Score 42.0**  
**A. WHAT:** Extend `_HADITH_MARKERS` and `_IJMA_MARKERS` in `phase3_deterministic.detect_evidence_refs` with additional high-precision Arabic scholarly patterns (e.g., common hadith citation verbs and ijmā’ phrasings used in fiqh/uṣūl). Then add unit tests that demonstrate (a) a set of known markers are captured via substring search, and (b) you don’t regress into boundary-based matching (the code explicitly warns that word boundaries are harmful in Arabic due to proclitics). fileciteturn34file0L1-L1  
**B. WHY:** Evidence extraction materially impacts study: students search and filter by evidence-bearing excerpts (“Qur’an”, “hadith”, “ijmā’”). False negatives hide important evidences and distort later taxonomy/synthesis weighting. fileciteturn34file0L1-L1  
**C. EVIDENCE:** The marker lists are currently small and hard-coded. fileciteturn34file0L1-L1 The code comments document a design decision that boundary checks are “catastrophically wrong” in Arabic—a fragile requirement that deserves regression tests. fileciteturn34file0L1-L1  
**D. DISCOVERY PATH:** `code_quality` (constants and missing coverage around high-stakes heuristics) and `test_health` (SPEC-rule coverage matrix) can surface this as a coverage gap. fileciteturn22file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Low (<1 day).  
**F. RISK IF UNFIXED:** The student’s evidence-centric reading workflows (finding Qur’an/hadith-based passages) become unreliable, and downstream engines may under-weight evidentiary excerpts. fileciteturn34file0L1-L1

12) **Prevent catastrophic long-run failures: add adaptive chunk time budgeting and checkpoint/retry for large sources**  
**Ranking:** Impact 7, Feasibility 5, Complexity High (3) → **Score 11.7**  
**A. WHAT:** Address batch-level stalls by adding a time-budgeting strategy at the chunk/phase level (especially for Phase 2 LLM steps): detect when a source is trending toward the run timeout and proactively (a) split overly large divisions into smaller chunks, (b) checkpoint intermediate artifacts so the run can resume without repeating expensive steps, and (c) cap per-source cost/time. This should be implemented in the excerpting orchestrator/parallel runner where phase timing is known. fileciteturn30file0L1-L1  
**B. WHY:** A student’s corpus is not a toy dataset: real books can be long, and a library that stalls on important works fails its primary purpose. Pipeline integrity here is user-visible: “this book never gets excerpted.” fileciteturn28file0L1-L1  
**C. EVIDENCE:** The smoke campaign shows at least one package (`taysir`) hit a hard `batch_timeout`, with 8 hours runtime and a very high cost estimate relative to other packages. fileciteturn28file0L1-L1 The processing log structure shows per-phase timings are tracked, indicating the system has an architectural place to implement budgets. fileciteturn30file0L1-L1  
**D. DISCOVERY PATH:** `creative` via “local_cost_efficiency” is the most likely to propose bounded throughput/cost improvements using repo evidence only. fileciteturn37file0L1-L1 `code_quality` may notice missing time budgets but may not connect them to real failures without reading `SUMMARY.json`. fileciteturn28file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** High (>3 days).  
**F. RISK IF UNFIXED:** Your summer work queue will be dominated by “why did excerpting stall?” incidents, and the student’s most important works may never become usable excerpts. fileciteturn28file0L1-L1

13) **Fix suspicious timing telemetry (identical per-chunk timings) so performance work is evidence-driven**  
**Ranking:** Impact 4, Feasibility 7, Complexity Medium (2) → **Score 14.0**  
**A. WHAT:** Investigate and correct the per-chunk timing reporting (`phase2a_per_chunk`, `phase2b_per_chunk`) where many chunks report the same float values. Add a simple sanity check: timings should vary across chunks; if variance is near zero, emit a telemetry-warning flag and store raw timers. This supports the cost/throughput improvements and prevents optimizing based on misleading data. fileciteturn30file0L1-L1  
**B. WHY:** While not directly visible to the student, accurate profiling is a prerequisite for reducing timeouts and improving throughput without quality regression. Better performance means more books processed, which is directly visible as corpus coverage. fileciteturn28file0L1-L1  
**C. EVIDENCE:** The processing log shows repeated identical per-chunk times across many divisions, which is implausible in real workload variation. fileciteturn30file0L1-L1  
**D. DISCOVERY PATH:** `code_quality` (telemetry correctness) and `creative` cost-efficiency template are both plausible. fileciteturn37file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Medium (1–3 days).  
**F. RISK IF UNFIXED:** You will waste summer development time chasing the wrong performance bottlenecks and remain vulnerable to timeouts on large works. fileciteturn28file0L1-L1

14) **Resolve “unknown” layer authors using `manifest.layer_map` to prevent attribution loss**  
**Ranking:** Impact 6, Feasibility 6, Complexity Medium (2) → **Score 18.0**  
**A. WHAT:** When `TextLayerSegment.author_canonical_id` is missing (and thus excerpting emits `author_id="unknown"`), consult upstream `manifest.layer_map` to infer the author for a layer type when it is unambiguous (e.g., exactly one `LayerInfo` exists for that `layer_type` with a known `author_canonical_id`). Apply this both to `primary_author_layer.author_id` and to `quoted_scholars[].resolved_name` in `compute_quoted_scholars`. This is deterministic and bounded: only fill when the mapping is unambiguous. fileciteturn34file0L1-L1  
**B. WHY:** Attribution integrity is central in Islamic study: students must know when a passage is matn vs sharḥ vs ḥāshiyah voice and, ideally, which scholar is speaking. “unknown” is sometimes unavoidable, but avoidable “unknown” is silent metadata corruption. fileciteturn15file0L1-L1  
**C. EVIDENCE:** Excerpting currently hard-falls back to `"unknown"` in deterministic attribution (`author_id=... or "unknown"`) and even warns that excluding same-(type, unknown) quote layers can lose scholar voice. fileciteturn34file0L1-L1 The normalization contracts include a `layer_map` with richer author metadata (canonical id, Arabic name, confidence). fileciteturn15file0L1-L1  
**D. DISCOVERY PATH:** `contract_boundaries` is best: it emphasizes metadata flow across engines, exactly where “layer_map exists but not used” becomes visible. fileciteturn22file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Medium (1–3 days).  
**F. RISK IF UNFIXED:** The student sees blurred lines between voices (“who said this?”), and downstream synthesis may attribute claims incorrectly or flatten scholarly disagreement. fileciteturn34file0L1-L1

15) **Create and maintain `engines/excerpting/KNOWN_LIMITATIONS.md` as a first-class artifact**  
**Ranking:** Impact 5, Feasibility 8, Complexity Low (1) → **Score 40.0**  
**A. WHAT:** Add `engines/excerpting/KNOWN_LIMITATIONS.md` and make it authoritative: every recurring excerpting failure mode discovered (e.g., V‑P3‑2 drop mechanics, micro-unit handling, consensus disagreement patterns) should be summarized with reproduction pointers and mitigation status. This file is already expected by the repo’s documentation freshness scanner prompt; implement it for excerpting specifically and keep it synced. fileciteturn22file0L1-L1  
**B. WHY:** For a student-facing scholarly library, “known limitations” are part of trust. More importantly for your autonomous audit, this becomes a durable memory substrate: the overnight system will re-discover the same issues unless they are explicitly recorded and referenced by backlog generation. fileciteturn33file0L1-L1  
**C. EVIDENCE:** The Codex task generator’s documentation scan explicitly names “KNOWN_LIMITATIONS.md files” as a target of freshness audits, implying they are part of the project’s expected documentation surface area. fileciteturn22file0L1-L1 The excerpting engine currently has a high-signal limitation (drops/EX‑V‑002) that deserves documentation tied to concrete artifacts (`validation_drops.jsonl`, `SUMMARY.json`). fileciteturn28file0L1-L1 fileciteturn29file0L1-L1  
**D. DISCOVERY PATH:** `spec_quality` and `documentation` are the likely discovery channels: missing expected limitation docs and missing “known gaps” anchoring for real failure signatures. fileciteturn22file0L1-L1  
**E. IMPLEMENTATION COMPLEXITY:** Low (<1 day).  
**F. RISK IF UNFIXED:** The autonomous system will repeatedly spend cycles re-deriving the same failure modes, and the student will have no explicit warning about the system’s current reliability boundaries. fileciteturn33file0L1-L1

## Meta-analysis

**Improvements no existing scanner is likely to discover (without changes).**  
The dominant excerpting failure signature (EX‑V‑002 frequency, micro-unit drops, and `ANO-UNIT-LOSS`) is most clearly visible in `integration_tests/smoke_api_v2/` artifacts. However, the current Codex generator’s “core” regression task runs `pytest engines/excerpting/tests/` and does not inherently parse smoke outputs. fileciteturn22file0L1-L1 fileciteturn28file0L1-L1 This implies a gap: **a “smoke-output audit” scanner** is needed—one that reads `SUMMARY.json`, `validation_drops.jsonl`, and anomaly logs, produces trend metrics (drop counts by error code and by structural pattern), and emits backlog items when thresholds are breached. This would turn real output evidence into Phase 3 ranked work items reliably, instead of depending on incidental code inspection. fileciteturn31file0L1-L1

**Improvements that span multiple engines (hardest to find with single-engine focus).**  
Several high-leverage fixes are cross-boundary:
- Using normalization’s `manifest.layer_map` to resolve “unknown” author attributions (normalization → excerpting). fileciteturn15file0L1-L1 fileciteturn34file0L1-L1  
- Lifting structured footnote/takhrīj metadata into excerpt-level fields (normalization → excerpting → taxonomy/synthesis). fileciteturn15file0L1-L1 fileciteturn12file0L1-L1  
- “Unit conservation” checks require reading Phase 2b groupings and Phase 3 outputs and comparing them (internal multi-phase boundary, and practically a tooling/integration concern). fileciteturn31file0L1-L1

**Arabic-specific issues a non-Arabic-specialist LLM may systematically miss.**  
Even strong general models can miss Arabic-specific text processing hazards that manifest as boundary errors rather than obvious “translation errors”:
- **Proclitic attachment** (“و”, “ف”, “ب”, “ك”, “ل”) breaks naïve word-boundary regex assumptions; the code explicitly documents that boundary checks are “catastrophically wrong” for markers like hadith/ijmā’ detection. fileciteturn34file0L1-L1  
- **Unicode codepoint vs grapheme length** (diacritics are separate codepoints) makes LLM-produced “first 80 chars” snippets unreliable; the current system directly suffers from this via V‑P3‑2 drops. fileciteturn32file0L1-L1  
- **Structural micro-units** (ordinal headings, “مسألة”, “تنبيه”, “فائدة”) can be extremely short yet essential; a model without Arabic reading intuition may treat them as discardable noise unless explicitly instructed and tested. fileciteturn29file0L1-L1  
The strategic implication is that excerpting needs more *deterministic, Arabic-aware* guardrails (pattern libraries + regression tests) to reduce dependence on LLM “taste” for segmentation quality. fileciteturn33file0L1-L1

**Creative templates: which are most/least likely to yield genuinely novel excerpting findings, and why.**  
The repository state does not match the brief’s “8 creative templates” for the Codex runtime: `overnight_codex/creative_templates/` currently contains **three** templates, all “local, repo-only” and all using `gpt-5.4`. fileciteturn35file0L1-L1 fileciteturn36file0L1-L1 fileciteturn37file0L1-L1 A broader template library exists under `overnight/creative_templates/` in a separate design doc, but it is not what `scripts/overnight_codex_task_generator.py` loads for the Codex-safe manifest. fileciteturn22file0L1-L1 fileciteturn25file0L1-L1

Within the three templates actually used by Codex:
- **Most likely to yield novel excerpting findings:** *Local Failure-Mode Review*—because it explicitly asks for “realistic local failure modes and missing guardrails,” which is exactly how issues like V‑P3‑2 drop semantics and unit-loss invariants get discovered. fileciteturn35file0L1-L1  
- **Second strongest:** *Local Architecture Challenge*—because excerpting’s multi-session state machine (Phase1 assembly → Phase2 classify/group → Phase3 deterministic/enrichment/consensus/validation) is exactly where over-complexity and “invariant gaps” hide; a good architecture challenge tends to surface “you need a conservation invariant” and “don’t use failure flags as placeholders.” fileciteturn36file0L1-L1  
- **Least likely (but still useful):** *Local Cost and Throughput Review*—it tends to produce optimization and batching suggestions (useful for preventing stalls like `taysir`), but it is less likely to surface *semantic* correctness errors (like micro-unit loss) unless it is explicitly coupled with smoke-output evidence. fileciteturn37file0L1-L1 fileciteturn28file0L1-L1  

A key strategic gap is that the current Codex-local creative set is **not adversarial and not Arabic-structure-specific** (and it forbids Arabic-domain judgment by design), so it will under-discover issues like list-structure attachment rules, edge punctuation, and scholastic discourse markers unless you add either (a) a new scanner that reads smoke outputs, or (b) a new Codex-safe template that asks specifically for “Arabic structural micro-unit failure modes” using only repository evidence (no web, no new content generation). fileciteturn35file0L1-L1 fileciteturn29file0L1-L1