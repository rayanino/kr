- **When HR-23 blocks an Agent dispatch, you MUST run `/prompt-architect` and retry the dispatch.** You may NOT substitute by doing the work yourself with direct tools (Read, Grep, Bash, Glob). The HR-23 hook exists because every prompt to every agent must be optimized before dispatch. Bypassing by "I'll do it myself" defeats the entire multi-model optimization principle.
- **The circumvention pattern to prevent:** HR-23 blocks Agent → CC says "I'll explore directly instead" → uses Read/Grep/Bash to do the agent's work → prompt is never optimized → the work product is lower quality because it wasn't reviewed by prompt-architect first.
- **The correct pattern:** HR-23 blocks Agent → CC runs `/prompt-architect` with the dispatch prompt → CC retries the Agent dispatch with the optimized prompt → agent executes with an optimized prompt.
- **No exceptions.** Not "it's a simple search." Not "I can do this faster myself." Not "the agent would just use the same tools." Every dispatch goes through `/prompt-architect`. Every time. Owner ALL-CAPS directive 2026-04-07.
