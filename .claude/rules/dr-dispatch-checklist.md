- **Before writing ANY Deep Research relay prompt**, verify access capabilities. **Repo-access claims for ChatGPT DR and Claude DR are PRIOR HYPOTHESES, not guarantees** — empirical fetch capability varies per session, per DR mode, per private-repo authentication state. The 2026-04-29 Phase 5 scholar-matching DR dispatch produced a Claude DR session that disclosed in its own process-honesty note: "the private repository `github.com/rayanino/kr` was not reachable through this researcher's fetch tooling: GitHub's authenticated raw URLs and `api.github.com/repos/...contents` endpoints are both rejected by the fetcher because they are not surfaced via prior search results, and the provided PAT cannot be exchanged for a public URL." Treat the prior hypotheses below accordingly.
  - **ChatGPT DR (Pro/Deep Research mode):** PRIOR HYPOTHESIS = HAS private repo access via GitHub. Verify per session — the DR's first-pass output should disclose whether repo fetches succeeded. If yes, file paths are sufficient. If no, the DR will fall back to treating prompt-quoted content as authoritative.
  - **Claude DR (Deep Research mode / `compass_artifact_*` mode):** PRIOR HYPOTHESIS = HAS private repo access via GitHub. **EMPIRICALLY FALSIFIED 2026-04-29** — fetch tooling rejected raw URLs and contents API; PAT could not be exchanged. Treat repo access as unconfirmed until the DR's own process-honesty note discloses the fetch outcome. If the DR's output lacks such a note, assume fetch failed and treat all repo-bound recommendations as PRELIMINARY-pending-verification.
  - **Gemini DR:** CANNOT access the repo. Prepare FILE UPLOADS (bundle key files for the owner to upload to the Gemini session).
  - **ChatGPT (non-research/Thinking mode):** CANNOT access the repo. Paste content or upload files.
  - **Gemini CLI:** Has LOCAL repo access via `gemini -p`. No relay needed.
- **Mandatory in every ChatGPT DR / Claude DR relay prompt: require the DR to lead with a process-honesty note disclosing fetch outcomes.** Add an explicit instruction: "Before producing the structured output, lead with a brief process-honesty note disclosing (a) which repo files you successfully fetched via your tooling, (b) which files you could not fetch and why, (c) for files in (b), what surface (prompt-quoted content / training-data priors / external search) you treated as authoritative instead." This makes silent-fallback impossible — the DR explicitly tags its own evidence base.
- **Fallback pattern when repo access fails (KR-side post-DR action):** When a DR's process-honesty note discloses fetch failure (or the DR produces no such note and its claims look brief-derived), the next-session digest MUST verify every repo-bound claim against actual repo content directly. Tag each verified claim as one of: ✓ verified-against-repo / ◐ uncited-but-plausible / ✗ contradicted-by-repo / ⊘ existing-not-mentioned. This produces a confidence-weighted digest where the DR's analytical work is preserved but its repo-content claims are independently confirmed.
- **NEVER paste full file contents into ChatGPT DR or Claude DR prompts.** This wastes the owner's time and effort. The owner explicitly said (2026-04-01): "NEVER make this mistake again." However, this rule applies to LARGE files (full SPECs, full code modules). For small load-bearing excerpts (5-30 lines from a specific atom or function), inline-quoting in the prompt is the only way to ensure the DR sees the exact content regardless of fetch capability — quote the smallest sufficient excerpt rather than referencing the whole file.
- **For Gemini DR file bundles:** Include the specific files needed for the task, not the entire repo. Keep bundles focused: questionnaire + SPEC + excerpt samples for evaluation tasks.
- **Pre-relay verification (KR-side, before owner pastes the prompt):** When dispatching a DR question that depends on repo content, verify two things first: (1) all repo files cited in the prompt are pushed to `origin/main` (DR fetch hits the remote, not local) — `git status -sb` should show zero unpushed commits; (2) the prompt EITHER inlines the smallest sufficient excerpts of load-bearing files OR has a fallback "if you cannot fetch this file, treat the following quoted excerpt as authoritative" inline section for each critical file. Without this dual posture, the DR's quality is hostage to fetch-tooling success.
- **Per-DR digest as standard workflow:** Regardless of fetch outcome, the next-session digest of any DR output MUST follow the standard structure: process-honesty note acknowledgment + Phase 1 first-principles + Phase 2 reconciliation with confidence tags + §2 matrix + chosen architecture + §3 deliverables with KR-side verification + identification of the single most surprising/contestable architectural choice (the synthesis pivot). Cross-provider synthesis (when ≥2 DRs are dispatched on the same question) operates on the digests, not on raw DR outputs — digests are the compaction-survivable analytical surface; raw DR outputs are point-in-time artifacts.
