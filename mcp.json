{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "Up-to-date library documentation. Use before generating code with Pydantic, LiteLLM, Instructor, FastAPI, BeautifulSoup, PyArabic."
    },
    "tavily": {
      "command": "npx",
      "args": ["-y", "tavily-mcp@latest"],
      "env": {
        "TAVILY_API_KEY": "${TAVILY_API_KEY}"
      },
      "description": "Web search, extraction, and research. Use for Islamic scholarly tools, Arabic NLP techniques, format documentation."
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "env": {
        "MEMORY_FILE_PATH": "${HOME}/.claude-memory/kr-engine.jsonl"
      },
      "description": "Persistent knowledge graph across sessions. Store architectural decisions, engine states, research findings."
    }
  }
}
