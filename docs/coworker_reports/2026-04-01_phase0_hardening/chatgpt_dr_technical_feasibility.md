# Technical Feasibility Review of the KR Owner Q&A Questionnaire for Excerpting

## Source integrity and scope check

This review is based on the **questionnaire template currently present in the repo** (the file titled “Owner Q&A Questionnaire — Excerpting Engine Calibration”), plus the excerpting engine’s Phase 1–3 implementation and the current Phase 2 prompts. fileciteturn66file0L1-L1 fileciteturn74file0L1-L1 fileciteturn31file0L1-L1 fileciteturn35file0L1-L1 fileciteturn69file0L1-L1 fileciteturn68file0L1-L1

Two path mismatches matter operationally:

- The repo contains `integration_tests/questionnaire/QUESTIONNAIRE_TEMPLATE.md` (not `OWNER_QUESTIONNAIRE.md`). fileciteturn66file0L1-L1  
- The grouping prompt implementation file is `engines/excerpting/src/phase2_group.py` (not `phase2b_group.py`). fileciteturn35file0L1-L1  

I could not locate `integration_tests/questionnaire/OWNER_QUESTIONNAIRE.md` or `integration_tests/questionnaire/QUESTIONNAIRE_EXAMPLES.md` in the current repo state using the available connector tooling. As a result:  
- the feasibility matrix below is keyed to the **template’s** question parts (a/b/c/d) plus five “implicit interactions” the template asks the team to stage during the owner session;  
- I cannot evaluate whether “why each excerpt was selected” is coherent, because that supporting artifact isn’t available in the repo snapshot I can access.

## Pipeline constraints that determine feasibility

The excerpting engine is currently a **single-pass, partitioning pipeline**:

- Phase 1 deterministically assembles chunks, merges tiny divisions, splits oversized divisions, preserves diacritics, renumbers footnotes, rebases text layers, and records join/boundary metadata. fileciteturn74file0L1-L1  
- Phase 2a calls one LLM per chunk to classify contiguous segments, with strict coverage invariants and snippet-anchored offset normalization. fileciteturn31file0L1-L1  
- Phase 2b calls one LLM per chunk to group segments into **teaching units**, explicitly enforcing decontextualization-prevention constraints (rule+evidence, question+answer, position+refutation, etc.) and self-containment grading (FULL/PARTIAL/DEPENDENT). fileciteturn35file0L1-L1  
- Phase 3 deterministically extracts the primary text substring (no reflow), computes evidence refs by pattern matching, computes layer attribution, etc., then runs one LLM enrichment call per chunk for topics, school attribution, scholar resolution, cross-references, takhrij, terminology variants, and `context_hint` for PARTIAL units. fileciteturn69file0L1-L1 fileciteturn68file0L1-L1  

Two constraints are especially decisive for “can we implement what the questionnaire implies?”:

- **Teaching units must form a non-overlapping partition of each chunk’s segments** (every segment is assigned to exactly one unit; units are contiguous in word space). This makes it inherently hard to deliver “separate excerpts” that each include shared context without changing the architecture to allow overlapping excerpts. fileciteturn61file0L1-L1  
- The current rules-and-contracts are tuned for a “reference-preserving” excerpting engine: the output `primary_text` is extracted as a substring and “never modified after extraction,” which conflicts with any preference that asks for rewritten/cleaned summaries **in the excerpting stage**. fileciteturn61file0L1-L1 fileciteturn69file0L1-L1  

A third practical constraint: `load_config()` currently returns defaults and does **not** load `config.yaml` or per-source overrides yet. That means any “calibration” is currently realized by code/prompt edits, not by configuration. fileciteturn73file0L1-L1

## Feasibility matrix per interaction

### Legend

- **ALREADY DONE**: Current pipeline already expresses this preference as implemented behavior (owner may not realize it).  
- **EASY**: Prompt tuning or simple threshold change inside current architecture; no likely >2x cost impact.  
- **HARD**: Requires new deterministic post-processing, schema changes, prompt branching by genre/science, or risks token/throughput issues that could approach >2x cost in worst cases.  
- **INFEASIBLE**: Conflicts with core architectural invariants (non-overlap partition), requires cross-engine capabilities (taxonomy/synthesis/UI), or requires rewriting original text (outside excerpting).

### Matrix

The interactions 1a–10d correspond to the template’s numbered sections and their question parts. fileciteturn66file0L1-L1  
Interactions 11–15 are “implicit interactions” the template asks the team to stage (e.g., “show it split into two excerpts”), which can imply feasibility expectations beyond the current pipeline.

| ID | Interaction (what a “strong preference” would demand) | Status | Why (pipeline mapping) | Flag if “strong preference” goes this way |
|---|---|---|---|---|
| 1a | Minimum excerpt size: “Short excerpts feel fragmentary; enforce a floor.” fileciteturn66file0L1-L1 | HARD | Current Phase 2b groups by conceptual unit without a minimum-length contract. Enforcing a minimum would likely need a **post-grouping merge** heuristic (deterministic) or iterative regrouping. Both are beyond pure prompt tuning and can fight self-containment and topic purity. fileciteturn35file0L1-L1 fileciteturn61file0L1-L1 | (c) architecture beyond prompt tuning (post-pass merge). Potential (b) if iterative re-LLM needed. |
| 1b | Maximum/topic splitting sensitivity: “Long excerpts cover too much; split aggressively.” fileciteturn66file0L1-L1 | EASY → HARD edge | Base grouping rules already discourage merging unrelated مسائل and default-split numbered items. fileciteturn35file0L1-L1 But “aggressive” splitting can increase units per chunk, inflating Phase 2b/3 outputs and risking token ceilings (GROUP_MAX_TOKENS / ENRICH max). fileciteturn61file0L1-L1 fileciteturn68file0L1-L1 | (b) possible if unit count pushes completion tokens toward 2×; (c) if multi-pass grouping becomes necessary. |
| 1c | Edge-case bias: prefer “too short” vs “too long.” fileciteturn66file0L1-L1 | EASY | This is directly expressible as a bias instruction in the Phase 2b grouping prompt (“when uncertain, split” vs “when uncertain, merge”). Current prompt already contains a strong “if uncertain, split” bias in several places. fileciteturn35file0L1-L1 | Usually none; prompt-only. |
| 2a | Self-containment baseline: “Can I understand without context?” fileciteturn66file0L1-L1 | ALREADY DONE (as a mechanism) | The pipeline already forces Phase 2b to label each unit FULL/PARTIAL/DEPENDENT using explicit criteria, and Phase 3 generates `context_hint` for PARTIAL. So the *measurement hook* exists. fileciteturn35file0L1-L1 fileciteturn68file0L1-L1 fileciteturn61file0L1-L1 | If owner expects “always FULL,” becomes HARD (see 9c). |
| 2b | What missing context should be shown alongside (what `context_hint` must contain). fileciteturn66file0L1-L1 | EASY | `context_hint` is already a first-class Phase 3 enrichment field for PARTIAL units, with fallbacks if omitted. Prompt tuning can steer its content format. fileciteturn68file0L1-L1 | None; prompt-only. |
| 2c | Back-references (“as mentioned earlier”): never allow dangling refs vs tolerate. fileciteturn66file0L1-L1 | HARD | Phase 2b’s self-containment criteria require resolving anaphora within the unit, but some references genuinely point outside the current chunk or require broader context than can be safely merged. Phase 3 can add `cross_references` metadata and `context_hint`, but it cannot *guarantee* full resolution without cross-chunk retrieval/linking. fileciteturn35file0L1-L1 fileciteturn68file0L1-L1 | (c) if the preference is “always resolve by showing the referenced text,” because that needs retrieval/UI integration or cross-chunk inclusion. |
| 3a | “Is side-by-side cross-book comparison useful?” fileciteturn66file0L1-L1 | INFEASIBLE (as excerpting capability) | Excerpting can emit metadata (topics, school, scholars) that *enables* later comparison. Phase 3 already produces school attribution and resolves scholars. fileciteturn68file0L1-L1 But the comparison experience itself is **retrieval/taxonomy/synthesis/UI**, not excerpting. | (c) cross-engine requirement. |
| 3b | “Make comparison work”: normalize excerpts to same “level of detail.” fileciteturn66file0L1-L1 | INFEASIBLE (as stated) | You can influence granularity and labeling, but “normalize across books” implies joint reasoning across sources and alignment against a shared taxonomy or canonical schema, which this pipeline does not do. fileciteturn73file0L1-L1 | (c) cross-engine alignment; likely needs taxonomy + synthesis contracts. |
| 4a | Definitions together vs split: “Always split linguistic vs technical definitions” or “keep together.” fileciteturn66file0L1-L1 | HARD | The repo has an explicit DR-1 spec (“Definition Pair Splitting”) marked **pending before implementation** and proposing cross-reference semantics. fileciteturn63file0L1-L1 Implementing DR-1 cleanly isn’t just prompt tuning; it wants companion linking and thresholds. | (c) because DR-1’s “companion” linking expectations imply schema/code changes (see 4b). |
| 4b | If split, must link the two definitions as companions. fileciteturn66file0L1-L1 | HARD | Current `CrossReference` schema lacks the richer relationship/type and target excerpt linkage DR-1 proposes, and Phase 3 cross-references are LLM-produced but not designed for deterministic “companion excerpt” pointers. fileciteturn61file0L1-L1 fileciteturn68file0L1-L1 fileciteturn63file0L1-L1 | (c) schema + deterministic linking logic (or prompt+UI conventions). |
| 4c | Study ordering: “linguistic first then legal” vs “both at once.” fileciteturn66file0L1-L1 | HARD (ambiguous ownership) | If “both at once,” the excerpting engine can keep them in one unit (prompt). If “separate but shown in a specific order,” that’s mostly a **UI/navigation** responsibility, since excerpt order is text order and excerpting is reference-preserving. fileciteturn69file0L1-L1 fileciteturn61file0L1-L1 | (c) if “ordering” means reordering content independent of source order. |
| 5a | Evidence must appear inline with the ruling. fileciteturn66file0L1-L1 | ALREADY DONE | Current Phase 2b rules include “Evidence cited for a ruling MUST stay with the ruling.” fileciteturn35file0L1-L1 | None. |
| 5b | Evidence should be separate (“tap to see evidence”). fileciteturn66file0L1-L1 | HARD | This conflicts with the current decontextualization rule that forces evidence to remain with the ruling. The repo’s DR-2 proposal would change this behavior and requires companion cross-references between ruling and evidence units. fileciteturn35file0L1-L1 fileciteturn63file0L1-L1 | (c) policy reversal + linking machinery; (b) possible if unit count balloons; (a) if “well known vs not” gating is required reliably. |
| 5c | For “well-known hadith,” reference may be enough (omit full text). fileciteturn66file0L1-L1 | INFEASIBLE (as excerpting output) | Excerpting extracts contiguous original text; it does not rewrite/abridge inside a span. You can choose boundaries, but you cannot reliably “omit quote text but keep reference” without either (i) splitting at the right boundary always, or (ii) rewriting the displayed excerpt. fileciteturn69file0L1-L1 fileciteturn61file0L1-L1 | (c) if the requirement is “always hide hadith text”; belongs to UI rendering or synthesis. |
| 6a | In khilaf, want all positions together. fileciteturn66file0L1-L1 | ALREADY DONE (intent) | Phase 2b prompt hard-requires keeping refutations with positions, verdicts with alternatives, questions with answers—exactly the constraints that keep khilaf unified when splitting would mislead. fileciteturn35file0L1-L1 | None. |
| 6b | Prefer each position as its own excerpt (navigable between). fileciteturn66file0L1-L1 | INFEASIBLE (strong form) | A clean “per-position excerpt” often requires repeating shared context across multiple excerpts. Current invariants require a **partition** (no overlap; each segment belongs to exactly one unit), blocking “duplicate the shared setup” approaches. fileciteturn61file0L1-L1 The DR-3 draft allows limited splitting only for very long, clearly separated blocks, not “always split.” fileciteturn63file0L1-L1 | (c) would require overlapping excerpts or multi-view representations. |
| 6c | Need explicit scholar/school attribution in debate. fileciteturn66file0L1-L1 | ALREADY DONE | Phase 3 enrichment extracts `school` and resolves scholars with roles (quoted opinion vs narrator etc.). Deterministic layer overlap also produces quoted-scholars provenance. fileciteturn68file0L1-L1 fileciteturn69file0L1-L1 | None. |
| 7a | Genre-specific behavior needed vs one-size-fits-all. fileciteturn66file0L1-L1 | HARD | Current Phase 2 prompts are constant and keyed only on `structural_format`, not on “science” (fiqh/nahw/hadith). Adding science-conditioned rules likely requires prompt branching and additional “genre contract” decisions. fileciteturn35file0L1-L1 fileciteturn31file0L1-L1 | (c) architecture extension (prompt branching). (b) if prompts grow substantially. |
| 7b | In nahw, include the شاهد/example sentence inside the excerpt vs separate. fileciteturn66file0L1-L1 | EASY | This is a grouping-boundary preference (keep example + explanation together vs separate). Phase 2b grouping rules already support “definition + examples = one unit,” and can be specialized for nahw. fileciteturn35file0L1-L1 | None (prompt-only). |
| 7c | In hadith, include isnad vs matn-only. fileciteturn66file0L1-L1 | HARD | Phase 2a segmentation currently mandates “isnad chain + matn = one segment,” preventing Phase 2b from separating them unless Phase 2a boundary rules change. fileciteturn31file0L1-L1 | (c) prompt+behavior change in Phase 2a; risk of new failure modes in offset normalization and segment integrity. |
| 8a | After an excerpt, what should be shown next. fileciteturn66file0L1-L1 | INFEASIBLE (excerpting) | This is a navigation/retrieval policy question. Excerpting can emit metadata, but cannot decide “next view” without product UI logic. fileciteturn73file0L1-L1 | (c) outside excerpting engine. |
| 8b | Prefer cross-scholar jumps vs continue same author. fileciteturn66file0L1-L1 | INFEASIBLE (excerpting) | Requires cross-source indexing/ranking and topic placement; excerpting merely produces records and topic keywords. fileciteturn68file0L1-L1 | (c) outside excerpting; taxonomy/search concern. |
| 8c | “Go deeper” (hashiyah on sharh) browsing. fileciteturn66file0L1-L1 | HARD | Excerpting already computes layer attribution (`primary_author_layer`) and identifies non-primary layer authors via overlap. fileciteturn69file0L1-L1 This enables downstream UI, but the linking/graph traversal is not part of excerpting. | (c) if interpreted as “build/serve the layer-navigation graph” rather than “provide enough metadata.” |
| 9a | With `context_hint`, is the hint enough? fileciteturn66file0L1-L1 | ALREADY DONE (mechanism) | `context_hint` exists and is enforced for PARTIAL units via Phase 3 enrichment with fallback behavior. Quality can be tuned, but the feature is implemented. fileciteturn68file0L1-L1 fileciteturn61file0L1-L1 | None unless owner demands “always perfect,” which becomes (a)/(c). |
| 9b | Prefer longer excerpt that includes context directly, even if mixing topics. fileciteturn66file0L1-L1 | HARD | This is essentially “merge to avoid PARTIAL,” which may be possible within a chunk but can conflict with the “don’t merge unrelated content” guardrails and can still fail if the missing context is outside chunk scope. fileciteturn35file0L1-L1 | (c) if it forces cross-chunk merging; (b) if it inflates excerpt sizes dramatically and harms downstream. |
| 9c | “Are partial excerpts acceptable?” (If owner says “never.”) fileciteturn66file0L1-L1 | HARD → INFEASIBLE edge | The pipeline already flags DEPENDENT units and can gate them. fileciteturn61file0L1-L1 But eliminating PARTIAL in all cases may be impossible without either (i) very large merged excerpts, or (ii) cross-chunk retrieval/inclusion, or (iii) rewriting into summaries. | (c) if interpreted as “never PARTIAL, no matter what,” because some dependencies aren’t locally resolvable while preserving original contiguous spans. |
| 10a | Walkthrough of study session (user model). fileciteturn66file0L1-L1 | INFEASIBLE (excerpting) | This is requirements discovery for the product; not an excerpting calibration lever. | (c) outside excerpting. |
| 10b | When owner asks a fiqh question, what should they see? fileciteturn66file0L1-L1 | INFEASIBLE (excerpting) | That’s retrieval/search/synthesis behavior. Excerpting contributes inventory and metadata only. fileciteturn73file0L1-L1 | (c) outside excerpting. |
| 10c | Study one book vs topic across books. fileciteturn66file0L1-L1 | INFEASIBLE (excerpting) | Cross-book study is a taxonomy/index/UI question; excerpting is upstream. | (c) outside excerpting. |
| 10d | Original text vs cleaned-up summary. fileciteturn66file0L1-L1 | ALREADY DONE (original) / INFEASIBLE (summary in excerpting) | The excerpting engine explicitly preserves the original text span as `primary_text` and does not rewrite it. fileciteturn61file0L1-L1 fileciteturn69file0L1-L1 If the owner wants “cleaned-up summary,” that is a different engine stage (synthesis), not excerpting. | (c) if summary is demanded as excerpting output. |
| 11 | (Implicit) Section 4 staging: “show split into two excerpts” on demand. fileciteturn66file0L1-L1 | HARD | Producing the split variant isn’t a UI toggle today; it requires actually implementing DR-1-like behavior (or at least a dev-only alternate mode). DR-1 is documented as pending. fileciteturn63file0L1-L1 | (c) requires alternate extraction mode or DR-1 implementation. |
| 12 | (Implicit) Section 5 staging: “show ruling alone” vs “ruling+evidence together.” fileciteturn66file0L1-L1 | INFEASIBLE (as a single pipeline run) | The pipeline produces exactly one partition of text per chunk. Showing two decompositions for the same passage (one including evidence, one excluding) is an **A/B mode** feature requiring multiple extraction runs or a UI layer that conditionally hides text. fileciteturn61file0L1-L1 fileciteturn73file0L1-L1 | (c) multiple-mode architecture or UI, not prompt-only. |
| 13 | (Implicit) Section 6 staging: “show khilaf full” then “individual positions as separate excerpts.” fileciteturn66file0L1-L1 | INFEASIBLE (strong form) | Same issue as (12) plus the non-overlap partition constraint blocks “separate positions + shared context” unless you allow overlap/duplication. fileciteturn61file0L1-L1 | (c) needs overlapping excerpts or a different representation for debate. |
| 14 | (Implicit) Section 3 staging: “pick 3 excerpts from different books on the same topic.” fileciteturn66file0L1-L1 | HARD | This presumes you have a reliable way to retrieve cross-book “same topic” excerpts. Excerpting produces `excerpt_topic` keywords but does not index/rank across sources; that’s downstream infrastructure. fileciteturn68file0L1-L1 | (c) outside excerpting; needs indexing/taxonomy/search. |
| 15 | (Implicit) “Each answer maps to a SPEC rule or prompt calibration” with traceability. fileciteturn66file0L1-L1 | HARD (process/tooling) | The repo currently doesn’t load config overrides and does not have a built-in traceability matrix generator. Achieving “every decision traces to an answer” is feasible as process, but not currently automated. fileciteturn73file0L1-L1 | (c) tooling/process beyond prompt tuning. |

## Where the questionnaire implies capabilities that the excerpting engine cannot deliver

Several sections blur “excerpting calibration” with “product experience across engines.” The most important mismatches:

The comparison and navigation sections attribute cross-book behaviors to “excerpting calibration,” but excerpting does not (and cannot) decide cross-source retrieval or side-by-side presentation. In the current pipeline, excerpting ends at writing excerpt records; comparison is downstream. fileciteturn73file0L1-L1

The questionnaire sometimes implies that the team can easily show **alternative decompositions** of the same text (“show as-is, then show split”), but the excerpting engine produces a **single partition** per run. Producing “as-is” vs “split” is an A/B mode problem, not a single prompt knob. fileciteturn61file0L1-L1 fileciteturn73file0L1-L1

The evidence questions present “tap to see evidence separately” as though it’s only an excerpt boundary question. But the current grouping prompt explicitly binds evidence to rulings to prevent decontextualization, and DR-2 proposes reversing that with explicit linking semantics—this is not “free.” fileciteturn35file0L1-L1 fileciteturn63file0L1-L1

The hadith isnad question implicitly suggests you can “just remove isnad,” but the Phase 2a segmentation rules currently treat isnad+matn as a single segment boundary constraint—meaning the system would need a Phase 2a rule change, not merely a Phase 2b grouping tweak. fileciteturn31file0L1-L1

## Owner preferences that are already implemented but the template doesn’t make obvious

These are “hidden wins” you can explicitly surface to the owner (or at least to the team) so the Q&A isn’t wasting bandwidth rediscovering capabilities:

Rule–evidence binding is already a first-class grouping constraint (“Evidence cited for a ruling MUST stay with the ruling”), which corresponds to a strong “inline evidence” preference. fileciteturn35file0L1-L1

Decontextualization prevention is already strongly encoded: refutations must stay with the positions they refute; verdict/tarjīḥ markers must stay with the alternatives; question/answer cycles must remain together. This is directly aligned with “don’t mislead me with ‘puzzle’ excerpts.” fileciteturn35file0L1-L1

Self-containment grading is already implemented as a structured output (FULL/PARTIAL/DEPENDENT) with notes, and PARTIAL units already trigger `context_hint` generation in Phase 3. fileciteturn35file0L1-L1 fileciteturn68file0L1-L1

“Original text exactly as written” is already a hard architectural property: `primary_text` is extracted as a substring from assembled text and not rewritten. So if the owner values fidelity, the engine already matches that requirement at the excerpting layer. fileciteturn69file0L1-L1 fileciteturn61file0L1-L1

Scholar and school metadata extraction is already implemented in Phase 3 enrichment with explicit role typing (quoted opinion vs narrator vs refuted position). If the owner wants to see “who says what,” the excerpting output already carries the raw material (even if the UI doesn’t expose it yet). fileciteturn68file0L1-L1

## Missing calibration targets

The template covers several critical dimensions (granularity, self-containment, evidence handling, khilaf, genre differences). But if the goal is to make the owner’s answers translate into **actionable, stable controls** for Phase 2/3 behavior, several high-leverage calibration targets are missing:

The questionnaire does not ask about the owner’s tolerance for “more excerpts” vs “fewer excerpts,” even though finer granularity increases total excerpt count and can materially affect downstream costs (indexing, review burden) and Phase 2b/3 token budgets. This is a necessary tradeoff axis when adopting DR-1/DR-2 style splitting. fileciteturn61file0L1-L1 fileciteturn68file0L1-L1 fileciteturn63file0L1-L1

Footnotes are operationally important in scholarly texts, and the pipeline already carries them forward and feeds them into enrichment, but the owner is never asked whether footnotes should be shown inline, collapsed, or treated as evidence/takhrij sources. fileciteturn74file0L1-L1 fileciteturn68file0L1-L1

The template doesn’t explicitly calibrate the “derived benefits / فوائد / ما يؤخذ من الحديث” behavior. Yet the grouping prompt contains a very specific rule set for derived-benefits sections (default split per numbered item; sub-20-word exception). Owner preference here could materially change excerpt boundaries and is worth direct confirmation. fileciteturn35file0L1-L1

Layer mixing (matn vs sharh vs hashiyah vs tahqiq) is not directly calibrated. The owner is asked if they want to “go deeper,” but not whether a single excerpt may mix layers, or whether excerpts should be layer-pure when possible. This matters because Phase 3 already computes layer attribution deterministically, and the grouping stage could choose to respect layer boundaries more aggressively. fileciteturn69file0L1-L1 fileciteturn74file0L1-L1

Error-tolerance preferences are missing: would the owner rather (i) drop low-confidence excerpts, (ii) include them but label them PARTIAL/DEPENDENT with warnings, or (iii) route them to human gate and hide them from normal browsing? The pipeline already has gate flags and a DEPENDENT gate toggle, but the owner’s tolerance should govern how aggressive gating is in practice. fileciteturn61file0L1-L1

Finally, the template’s “traceability requirement” is stated as process (“each answer maps to a SPEC rule”), but the questionnaire does not define how to resolve conflicts when answers trade off against each other (e.g., “never partial” vs “never mix topics” vs “always show evidence”). Without an explicit conflict-resolution question, translations can become ad hoc. fileciteturn66file0L1-L1

