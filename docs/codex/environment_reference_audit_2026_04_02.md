# Environment Reference Audit — 2026-04-02

## Scope

- `CLAUDE.md`
- `.claude/rules/*.md`
- `.claude/agents/*.md`

## Result

No blocking stale file references were found in the audited scope.

The durable repo paths most likely to have drifted are currently valid, including:

- `reference/archive/STEERING.md`
- `reference/TESTING_FRAMEWORK.md`
- `reference/ENTRY_EXAMPLE.md`
- `reference/archive/VISION.md`
- `.claude/skills/arabic-text/SKILL.md`
- `.claude/skills/technology-survey/SKILL.md`
- `.claude/skills/coworker-dispatch/SKILL.md`
- `engines/excerpting/CLAUDE.md`

## Missing Paths That Are Forward-Looking, Not Stale

These paths are absent in the current checkout, but the referencing files describe them as generated outputs or future run artifacts rather than canonical source files:

1. `.claude/rules/ri-trace-collection.md`
   - Missing path: `eval/action_plan.md`
   - Reason: explicitly described as generated by `recursive-improve eval`

2. `.claude/rules/llm-call-optimization.md`
   - Missing path: `tests/results/source_engine/ERROR_LOG.json`
   - Reason: explicitly described as created on first API error

3. `.claude/agents/model-researcher.md`
   - Missing path: `overnight/results/model-research/findings.md`
   - Reason: output target for a later research run

## Non-Reference Environment Drift Observed

This audit found one real environment problem outside the stale-reference class:

- At session start, the WSL checkout at `/home/rayane/kr-codex` did not contain `integration_tests/smoke_api_v2/`, while the Windows checkout at `C:\\Users\\Rayane\\Desktop\\kr` did.
- Both checkouts were on the same commit: `fd9d5232` on `master`.
- This was a data-lane divergence, not a branch mismatch and not a broken documentation reference.
- For weekend execution from the preferred WSL lane, only the safe non-raw, non-cache `smoke_api_v2` artifacts were mirrored into WSL on 2026-04-02.

## Audit Notes

- Slash commands such as `/smart-compact`, `/catchup`, and `/ratchet` were excluded from the stale-file check because they are command references, not repo paths.
- Placeholder templates such as `engines/<n>/...` and `PHASE_N_...` were excluded because they are intentionally parameterized patterns.
